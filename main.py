from conversion import *

if __name__ == "__main__":
    # dir
    dir = ""

    # png -> jpg
    # traverse(dir, ".png", compress_image, "original", "jpg")

    # jpg -> 4k
    # traverse(dir, ".jpg"/, compress_image, "3840:2160", "jpg")

    # jpg -> 1080p
    # traverse(dir, ".jpg", compress_image, "1920:1080", "jpg")

    # mp4 -> 4k
    # traverse(dir, ".mp4", compress_video, "3840:2160", 60)

    # mp4 -> 1080p
    # traverse(dir, ".mp4", compress_video, "1920:1080", 60)
